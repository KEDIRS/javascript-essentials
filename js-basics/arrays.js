const fruits = ["Apple", "Banana", "Mango","Papaya"];// Declaring an array 'fruits' containing four fruit names.

for (const fruit of fruits)// I have Used a 'for...of' loop to iterate through each element in the 'fruits' array. 
{
    console.log(fruit);
}// Log each fruit to the console during each iteration.
